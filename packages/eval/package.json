{
  "name": "@securerag/eval",
  "private": true,
  "version": "0.1.0",
  "description": "SecureRAG evaluation: independent authorization oracle, T3 corpus, gate tests",
  "type": "module",
  "engines": {
    "node": ">=22.22"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@securerag/core": "^0.1.0",
    "@securerag/providers": "^0.1.0"
  },
  "devDependencies": {
    "@securerag/db": "^0.1.0",
    "@types/pg": "^8.15.4",
    "testcontainers": "^12.1.0",
    "tsx": "^4.23.6",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "@securerag/api": "^0.1.0",
    "@securerag/security": "^0.1.0"
  }
}